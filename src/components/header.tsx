import { Typography, TypographyProps } from "@mui/material";
import React from "react";

export default function Header(props: TypographyProps) {
    return <Typography {...props} textAlign='center'/>
}
